import { View, Text } from 'react-native'
import React from 'react'
import SidebarMenu from '../drawer/SidebarMenu'

const Main = () => {
  return (
    <View style={{flex: 1}}>
        <SidebarMenu />
    </View>
  )
}

export default Main